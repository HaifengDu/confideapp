import { IPaymentState } from "../../interface/IPaymentState";
import IRootState from "../../interface/IRootState";
import { Action } from "vuex";

export const test:Action<IPaymentState,IRootState> = ({dispatch,commit},payload)=>{
    //
}
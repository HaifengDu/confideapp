import { IHomeState } from "../../interface/IHomeState";
import IRootState from "../../interface/IRootState";
import { Action } from "vuex";

export const test:Action<IHomeState,IRootState> = ({dispatch,commit},payload)=>{
    //
}